# Copy to .env.local and fill in values for local runs
# Vercel: set these in Project Settings → Environment Variables

# Optional shared secret for Grow webhook validation
# If set, incoming requests must send the same value in `x-grow-signature`
GROW_WEBHOOK_SECRET=

# Email sending
# Preferred: SMTP (set these to use Gmail or your provider)
EMAIL_USER=
EMAIL_PASSWORD=
# Optional SMTP overrides (defaults: gmail host when EMAIL_USER ends with @gmail.com)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_SECURE=

# Fallback provider: Resend (used when SMTP is not configured)
RESEND_API_KEY=
# Sender name/address. Defaults to EMAIL_USER if set.
EMAIL_FROM="AWAKENING <noreply@abyk.online>"

# Client fallback (EmailJS) — optional and not recommended for prod
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=
