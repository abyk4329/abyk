---
import ShareButton from '../components/ShareButton';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Awakening by Ksenia - גלו את קוד העושר שלכם"
  description="גלו את קוד העושר האישי שלכם - מסע להבנה עמוקה של הפוטנציאל הייחודי שלכם"
>
  <div class="container">
    <!-- Hero Section -->
    <section class="hero">
  <div class="card-surface card-stack hero-card">
        <h1 class="CardTitleLong">
          גלו את קוד העושר שלכם
        </h1>
        
        <p class="LeadText">
          כלי נומרולוגי המבוסס על תאריך לידה. כל ספרה נושאת תדר אנרגטי ייחודי, והאופן שבו הן משתלבות בקוד האישי יוצר דפוס, המשקף את הפוטנציאל והאתגרים ומספק כיוון ברור להגשמה ושפע.
        </p>
        
        <a href="/tools/wealth-code/calculator" class="btn btn-cta hero-cta ButtonPrimaryText">
          מחשבון קוד העושר
        </a>
      </div>
    </section>

    <!-- Share Section -->
    <section class="share-section">
  <div class="card-surface card-stack">
        <p class="LeadText">
          שלחו לחברים ולמשפחה הזמנה לגלות את עצמם בלחיצה אחת
        </p>
        <div class="share-button-wrapper">
          <ShareButton client:load />
        </div>
      </div>
    </section>


  </div>
</BaseLayout>

<style>
  /* Mobile First Styles */
  .container {
    padding: var(--card-padding-mobile-y) var(--card-padding-mobile-x);
  }

  /* Hero Section */
  .hero {
    min-height: auto;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 0;
  }

  .hero-card {
    display: flex;
    flex-direction: column;
    align-items: center; /* מרכז את כל התוכן */
    gap: var(--card-content-gap);
  }

  .hero-card h1 {
    margin: 0;
  }

  .hero-card .LeadText {
    margin: 0;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--btn-padding-y) var(--btn-padding-x);
    min-height: 3.25rem;
    text-decoration: none;
    -webkit-tap-highlight-color: transparent;
    font-weight: 600 !important;
    margin-top: var(--space-sm); /* 8px - מרווח נוסף לפני הכפתור */
  }

  /* Share Section */
  .share-section {
    margin-top: var(--card-section-gap-large);
    padding: 0;
  }

  .share-section .card-surface {
    gap: var(--card-content-gap);
  }

  .share-section .LeadText {
    margin: 0;
  }

  .share-button-wrapper {
    display: flex;
    justify-content: center;
    margin: 0;
  }

  /* Desktop Styles */
  @media (min-width: 768px) {
    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: var(--card-padding-desktop) var(--card-padding-mobile-x);
    }

    .hero {
      min-height: auto;
      padding: var(--space-xl) 0;
    }

    .hero-card {
      gap: var(--card-content-gap);
    }

    .hero-cta {
      padding: var(--btn-padding-y) var(--space-3xl);
      margin-top: var(--space-md); /* 16px */
    }

    .hero-cta:hover {
      transform: translateY(-3px);
    }

    .hero-cta:active {
      transform: translateY(-1px);
    }

    .share-section {
      margin-top: var(--card-section-gap-large);
      padding: 0;
    }

    .share-section .card-surface {
      gap: var(--card-content-gap);
    }
  }
</style>
