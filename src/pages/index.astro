---
import ShareButton from '../components/ShareButton';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Awakening by Ksenia - גלו את קוד העושר שלכם"
  description="גלו את קוד העושר האישי שלכם - מסע להבנה עמוקה של הפוטנציאל הייחודי שלכם"
>
  <div class="container">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-card">
        <h1 class="CardTitleLong">
          גלו את קוד העושר שלכם
        </h1>
        
        <p class="LeadText">
          כלי נומרולוגי המבוסס על תאריך לידה. כל ספרה נושאת תדר אנרגטי ייחודי, והאופן שבו הן משתלבות בקוד האישי יוצר דפוס, המשקף את הפוטנציאל והאתגרים ומספק כיוון ברור להגשמה ושפע.
        </p>
        
        <a href="/tools/wealth-code/calculator" class="btn btn-cta hero-cta ButtonPrimaryText">
          מחשבון קוד העושר
        </a>
      </div>
    </section>

    <!-- Share Section -->
    <section class="share-section">
      <div class="share-card">
        <p class="LeadText">
          שלחו לחברים ולמשפחה הזמנה לגלות את עצמם בלחיצה אחת
        </p>
        <div class="share-button-wrapper">
          <ShareButton client:load />
        </div>
      </div>
    </section>


  </div>
</BaseLayout>

<style>
  /* Mobile First Styles */
  .container {
    padding: var(--card-padding-mobile-y) var(--card-padding-mobile-x);
  }

  /* Hero Section */
  .hero {
    min-height: auto;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 0;
  }

  .hero-card {
    width: 100%;
    background: rgb(var(--color-surface));
    border-radius: var(--radius-xl);
    padding: var(--card-padding-mobile-y) var(--card-padding-mobile-x);
    text-align: center;
    box-shadow: var(--shadow-raised-min);
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--btn-padding-y) var(--btn-padding-x);
    min-height: 3.25rem;
    text-decoration: none;
    -webkit-tap-highlight-color: transparent;
    font-weight: 600 !important;
  }

  /* Share Section */
  .share-section {
    margin-top: var(--card-section-gap-small);
    padding: 1rem 0;
  }

  .share-card {
    background: rgb(var(--color-surface));
    border-radius: var(--radius-xl);
    padding: var(--card-padding-mobile-y) var(--card-padding-mobile-x);
    text-align: center;
    box-shadow: var(--shadow-raised-min);
  }

  .share-button-wrapper {
    display: flex;
    justify-content: center;
    margin-bottom: var(--space-md);
  }

  /* Desktop Styles */
  @media (min-width: 768px) {
    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: var(--card-padding-desktop) var(--card-padding-mobile-x);
    }

    .hero {
      min-height: auto;
      padding: var(--space-xl) 0;
    }

    .hero-card {
      padding: var(--card-padding-desktop);
    }

    .hero-cta {
      padding: var(--btn-padding-y) var(--space-3xl);
    }

    .hero-cta:hover {
      transform: translateY(-3px);
    }

    .hero-cta:active {
      transform: translateY(-1px);
    }

    .share-section {
      margin-top: var(--card-section-gap-large);
      padding: var(--space-xl) 0;
    }

    .share-card {
      padding: var(--card-padding-desktop);
    }

    .share-button-wrapper {
      margin-bottom: var(--space-lg);
    }
  }
</style>
