// This component has been replaced by the bespoke ThemeToggle implementation.
// Keeping an empty module here prevents import errors in projects that may
// still reference `NeuPowerSwitch` while the new toggle rolls out.

export default function NeuPowerSwitch() {
  return null;
}
